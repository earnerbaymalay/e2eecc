package com.cypherchat.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark palette (primary) ─────────────────────────────────────────────────
val CipherBlack    = Color(0xFF07090F)
val CipherNavy     = Color(0xFF0D1117)
val CipherSurface  = Color(0xFF161B27)
val CipherCard     = Color(0xFF1E2535)
val CipherBorder   = Color(0xFF2A3145)

val CipherTeal     = Color(0xFF00D4AA)   // Primary accent — "encrypted" teal
val CipherTealDim  = Color(0xFF008F72)
val CipherTealSoft = Color(0xFF00D4AA26) // 15% alpha

val CipherAmber    = Color(0xFFFFA500)   // Warning / unverified
val CipherRed      = Color(0xFFFF4757)   // Error / destructive
val CipherGreen    = Color(0xFF2ED573)   // Delivered / verified

val TextPrimary    = Color(0xFFE8EAF0)
val TextSecondary  = Color(0xFF8B93A8)
val TextMuted      = Color(0xFF4A5268)

// Bubble colors
val BubbleOutgoing = Color(0xFF003D2E)   // Dark teal — outgoing messages
val BubbleIncoming = Color(0xFF1E2535)   // Dark card — incoming messages

// ── Light palette (optional, not used by default) ─────────────────────────
val CipherLightBg      = Color(0xFFF4F6FA)
val CipherLightSurface = Color(0xFFFFFFFF)
